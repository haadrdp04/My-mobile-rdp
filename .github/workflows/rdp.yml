name: Windows-Mobile-RDP-Tailscale
on: workflow_dispatch

jobs:
  build:
    runs-on: windows-latest
    steps:
    - name: Download and Install Tailscale
      run: |
        Invoke-WebRequest -Uri "tailscale.com" -OutFile "tailscale-setup.exe"
        Start-Process -FilePath ".\tailscale-setup.exe" -ArgumentList "/qn" -Wait
    - name: Connect to Tailscale Network
      run: |
        & "C:\Program Files\Tailscale\tailscale.exe" up --authkey=${{ secrets.TAILSCALE_AUTH_KEY }}
    - name: Configure Secure Password
      run: |
        net user runneradmin MobileRDPPassword123!
    - name: Activate Windows RDP
      run: |
        Set-ItemProperty -Path 'HKLM:\System\CurrentControlSet\Control\Terminal Server' -Name "fDenyTSConnections" -Value 0
        Enable-NetFirewallRule -DisplayGroup "Remote Desktop"
    - name: Keep Server Alive
      run: |
        Start-Sleep -Seconds 21600
        
